{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits",
    ":maintainLockFilesWeekly"
  ],
  "timezone": "America/New_York",
  "schedule": ["before 5am on monday"],
  "labels": ["dependencies"],
  "commitBody": "Signed-off-by: renovate[bot] <29139614+renovate[bot]@users.noreply.github.com>",
  "packageRules": [
    {
      "description": "Group all balena-sdk-related deps",
      "groupName": "balena packages",
      "matchPackageNames": [
        "/^balena-/"
      ]
    },
    {
      "description": "Auto-merge devDependency patch updates",
      "matchDepTypes": ["devDependencies"],
      "matchUpdateTypes": ["patch"],
      "automerge": true
    },
    {
      "description": "Pin GitHub Actions to SHAs",
      "matchManagers": ["github-actions"],
      "pinDigests": true
    },
    {
      "description": "Group all docker/* GitHub Actions",
      "matchManagers": ["github-actions"],
      "matchPackageNames": ["docker/**"],
      "groupName": "docker actions"
    },
    {
      "description": "Group all actions/* (official GitHub) Actions",
      "matchManagers": ["github-actions"],
      "matchPackageNames": ["actions/**"],
      "groupName": "github actions"
    },
    {
      "description": "Group security / supply-chain GitHub Actions",
      "matchManagers": ["github-actions"],
      "matchPackageNames": [
        "github/codeql-action",
        "ossf/scorecard-action",
        "aquasecurity/trivy-action",
        "sigstore/cosign-installer",
        "anchore/sbom-action",
        "softprops/action-gh-release"
      ],
      "groupName": "security actions"
    },
    {
      "description": "Hold TypeScript on v6 until @balena/lint (typescript-eslint) supports v7",
      "matchManagers": ["npm"],
      "matchPackageNames": ["typescript"],
      "allowedVersions": "<7.0.0"
    },
    {
      "description": "Limit alpine base image lookups to semver versions (suppresses date-snapshot tag noise)",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["alpine"],
      "allowedVersions": "/^[0-9]+\\.[0-9]+(\\.[0-9]+)?$/"
    },
    {
      "description": "Stay on the Alpine 3.24 series: the 3.24 tag already floats to the newest 3.24.x, so only the 3.25 minor is ever proposed. Park it on the dashboard until we choose to move.",
      "matchDatasources": ["docker"],
      "matchPackageNames": ["alpine"],
      "matchUpdateTypes": ["minor", "major"],
      "dependencyDashboardApproval": true
    },
    {
      "description": "Alpine keeps only the newest -rN of a package per release branch, so a stale apk pin fails the build. Ship these promptly instead of waiting for the weekly run.",
      "matchDatasources": ["repology"],
      "groupName": "alpine packages",
      "schedule": ["at any time"],
      "automerge": true,
      "automergeType": "pr",
      "automergeStrategy": "squash"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Alpine packages annotated with `# renovate: alpine-package=<name>` in Dockerfile.template",
      "managerFilePatterns": [
        "/(^|/)Dockerfile(\\.template)?$/"
      ],
      "matchStrings": [
        "#\\s*renovate:\\s*alpine-package=(?<depName>[\\w-]+)\\s*\\n\\s*ARG\\s+\\w+_VERSION=(?<currentValue>[\\d.]+-r\\d+)"
      ],
      "datasourceTemplate": "repology",
      "versioningTemplate": "loose",
      "packageNameTemplate": "alpine_3_24/{{depName}}"
    }
  ]
}
